name: Deploy
on:
  push:
    branches:
      - main
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install deps
        run: |
          sudo apt-get update
          sudo apt-get install libasound2-dev libudev-dev libcairo2-dev libglib2.0-dev -y

      - name: Build
        uses: actions-rs/cargo@v1
        with:
          use-cross: true
          command: build
          args: --release --target x86_64-unknown-linux-gnu # do box is 64-bit ubuntu
      - name: Install SSH Key
        uses: shimataro/ssh-key-action@v2
        with:
          # https://zellwk.com/blog/github-actions-deploy/
          # private github key
          key: ${{ secrets.DEPLOY_KEY }}
          known_hosts: "just-a-placeholder-so-we-dont-get-errors"
      - name: Add to known hosts
        # SSH_HOST is the IP of the digital ocean droplet
        run: ssh-keyscan -H ${{ secrets.SSH_HOST }} >> ~/.ssh/known_hosts
      - name: Copy files
        run: rsync -avz ./target/release/chord-web ${{secrets.SSH_USER}}@${{secrets.SSH_HOST}}:${{secrets.DEST_FOLDER}}
